/* eslint-disable */
// @ts-expect-error this provies a JS template and therefore can not contain any TS traces/types.
function benoetigteElfen(flottenGroesse) {
	return Math.ceil(2.3);
}

export default benoetigteElfen;
