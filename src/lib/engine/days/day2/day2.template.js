/* eslint-disable */
// @ts-expect-error this provies a JS template and therefore can not contain any TS traces/types.
function findeDasStockwerk(input) {
	// wiederhole für jeden Buchstaben
	for (let character of input) {
		// git den Buchstaben auf der Console aus
		console.log(character);
	}
}

export default findeDasStockwerk;
